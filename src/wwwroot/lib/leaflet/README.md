Leaflet 1.9.4 is vendored here to match the existing `wwwroot/lib` asset pattern.

Upgrade by replacing this directory with the contents of `node_modules/leaflet/dist`
or the official Leaflet distribution for the pinned version.
